import { Inter, Roboto } from "next/font/google";

export const roboto = Roboto({
    subsets: ["latin"],
    weight: '400',
});

export const inter = Inter({
    subsets: ['latin'],
    weight: '400'
})
